tokens.env
.cache/
data/serp-history/
.DS_Store
server/config.yaml
__pycache__/

# SQLite runtime artifacts for filters MCP tool.
# Canonical data lives in server/data/filter_values.json (tracked in git).
server/data/filters.db
server/data/filters.db-shm
server/data/filters.db-wal
server/data/filters.db-journal
server/data/filter_values.json.tmp

# Change history DB is runtime state (per deployment).
# Each environment accumulates its own history; no canonical seed.
server/data/change_history.db
server/data/change_history.db-shm
server/data/change_history.db-wal
server/data/change_history.db-journal

# Secrets / local-only tokens.
metrika-token.txt

# Agent scratch.
.claude/worktrees/

# External material — third-party skill kept locally for reverse-engineering
# reference; not shipped.
yandex-performance-ops/

# Python venv for excalidraw-diagram renderer (created by `uv sync`).
.claude/skills/excalidraw-diagram/references/.venv/

# Other AI agents (keep only .claude/)
.agents/
.cline/
.codex/
.cursor/
.gemini/
.openclaw/
.windsurf/
.vscode/

# Built MCP server binary (produced by `go build` / Dockerfile).
leadgen-mcp.exe
leadgen-mcp-linux

# Campaign previews — RSYA generation artifacts (R6.5).
# Contains helper scripts with API tokens + large PNG/JSON payloads.
# Folder is mapped into the container at /app/previews (see docker-compose.yml).
docs/campaign_previews/

# Personal Claude Code permissions (per-clone).
.claude/settings.local.json

# Rendered exports from excalidraw files — source .excalidraw is canonical.
docs/*.png
