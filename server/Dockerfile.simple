FROM alpine:3.20
RUN apk --no-cache add ca-certificates
WORKDIR /app
COPY leadgen-mcp-linux ./leadgen-mcp
RUN chmod +x ./leadgen-mcp
EXPOSE 8080
ENTRYPOINT ["./leadgen-mcp"]
CMD ["-config", "/app/config.yaml"]
